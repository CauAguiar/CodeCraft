spring.application.name=demo

# Configuração do banco de dados MySQL
spring.datasource.url=jdbc:mysql://mysql-2399da87-cauraguiar-3b72.g.aivencloud.com:12044/defaultdb?useSSL=true&serverTimezone=UTC&requireSSL=true
spring.datasource.username=avnadmin
spring.datasource.password=AVNS_dVxWdCL8W1JFYuEpoR0

# Configuração do JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Caminho para o certificado CA no formato PEM
spring.datasource.hikari.data-source-properties.sslCertificateKeyStore=/ca.pem
spring.datasource.hikari.data-source-properties.sslMode=REQUIRED
spring.datasource.hikari.data-source-properties.verifyServerCertificate=true

# Permite que o servidor ouça em todas as interfaces de rede
server.address=0.0.0.0
# porta
server.port=8000